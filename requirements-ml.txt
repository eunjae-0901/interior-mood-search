# RTX 5090(Blackwell, sm_120)은 CUDA 12.8+ 지원 torch 빌드가 필요.
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128 로 먼저 설치 확인 권장
torch
torchvision
transformers
pillow
numpy
pandas
scikit-learn
umap-learn
matplotlib
tqdm
deep-translator

# Model 1 이미지 생성 (SDXL + ControlNet(canny+depth) + IP-Adapter) — 로컬 GPU(RTX 5090) 전용
diffusers>=0.31.0
accelerate
safetensors
controlnet-aux
opencv-python
invisible-watermark
